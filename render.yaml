services:
  - type: web
    name: checkmate
    env: python
    buildCommand: echo "forcing change for Git Desktop"
    startCommand: echo "temporary start command"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
# TEMP FORCED EDIT - TO BE REPLACED IMMEDIATELY
